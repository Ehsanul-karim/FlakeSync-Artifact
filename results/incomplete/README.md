# Incomplete Attempts

## Java-WebSocket

The Stage-1 log reached an initial candidate set of 639 locations. The attempt was stopped because it was taking too long, so there is no valid stage timing, critical result, or barrier result.

## HTTP Core

The attempt failed during setup because `/home/java8-flakesync/scripts/projects-For-Delta/apache/httpcore` was absent. This is an environment/setup failure, not evidence that FlakeSync cannot repair the test.

